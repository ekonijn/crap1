#!/bin/sh
fatal() { echo "$@"; exit 1; }
logged() { echo "+" "$@" ; "$@"; }
cmd() { logged "$@" || fatal "fail:" "$@"; }

export PATH=/opt/java/jdk/bin:$PATH
cmd cd /tmp/src
[ -x .s2i/run ] && logged exec .s2i/run
exec ./main
